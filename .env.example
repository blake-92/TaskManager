# En local no hace falta: sin VITE_API_URL el frontend usa http://localhost:3000.
# En la nube, ponlo con la URL pública del backend (Vite solo expone variables VITE_).
VITE_API_URL="https://tu-backend.onrender.com"
